# import flask

default_port = 800

port = default_port

link = "localhost://" + str(port)#localhost 800, 1000, 1200 , 1400........

content = None (link) # ToDo: implement null Request

while content is not None:
    port += 200
else: # comes to else if the port is Empty and hence generates the link
    port = port
    print(True)
def generate():
    return port